<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- App standalone: gira sul watch senza bisogno di un'app companion sul telefono -->
    <uses-feature android:name="android.hardware.vibrate" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- HR continuo durante l'allenamento (Health Services, step 6) -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <!-- Servizio in primo piano per la durata dell'allenamento: senza, con lo schermo
         spento il processo viene sospeso e i campioni del battito si perdono -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <!-- Serve solo a mostrare la notifica "allenamento in corso": se negata il servizio
         funziona lo stesso, semplicemente la notifica non compare -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- allowBackup=false: la sessione attiva (HR compreso) e i token di auth non devono
         finire nei backup cloud del dispositivo — i dati veri stanno gia' su Firestore -->
    <application
        android:name=".GymWatchApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.DeviceDefault">

        <!-- Dichiarazione obbligatoria per le app Wear OS standalone -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <uses-library
            android:name="com.google.android.wearable"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@android:style/Theme.DeviceDefault"
            android:screenOrientation="nosensor"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".sensors.WorkoutForegroundService"
            android:exported="false"
            android:foregroundServiceType="health" />

    </application>
</manifest>
